dist
node_modules
.pnp.*
.yarn/*
!.yarn/releases
!.yarn/plugins
!.yarn/sdks
!.yarn/versions
.DS_Store
.env
.env.local
*.local
.vite
coverage
# Raw automation runs — promoted into PRs selectively, never committed wholesale.
measurements
